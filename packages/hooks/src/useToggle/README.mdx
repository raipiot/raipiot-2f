---
name: "useToggle"
description: "A hook to toggle two values, with a function to set a target value."
---

```typescript
const [value, toggle, setValue] = useToggle({
  initValue: 0,
  reverseValue: 1,
})
```
